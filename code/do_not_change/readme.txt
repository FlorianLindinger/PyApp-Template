code from PyApp-Template (*gitlink) originally started by Florian Lindinger

Batch file tips/comments:
1) "@REM" indicates the start of a comment (use "&@REM" for comments at 
	the end of a code line, unless the line starts a nested sequence like a 
	line with IF/ELSE/FOR/..., e.g., "IF A==B ( @REM comment")