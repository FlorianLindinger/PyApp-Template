# tmp.txt files being created as part of a batch file
tmp.exe
# file generated that lists packages the python files need
python_file_requirements.txt
# tmp env folder that is needed to determine packages the python files need
tmp_env_for_install_package_from_files/
# compilation build folder
*.build