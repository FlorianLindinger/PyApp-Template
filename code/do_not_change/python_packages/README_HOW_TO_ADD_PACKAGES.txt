How to install a package for the embeddable Python backend. 
In a terminal use command:

py -{python version in P\P.exe} -m pip install {package name} --target "{target folder: if you are in this dir use: "."}"

e.g.

py -3.12 -m pip install pipreqs --target .
